export default function (key, hooks) {



    return {

        onFetch (config) {

        },

        onSuccess (...args) {
        },

        onError (...args) {
        },

        onComplete (...args) {
        },

        onCancel (...args) {
        }

    }
}